/**
 * :construction: (imlucas) I find myself running this
 * bash command on every project, almost every morning:
 *
 * ```bash
 * npm-check-updates-u && npm install && check && test
 * ```
 *
 */
// var taskmgr = require('../lib/taskmgr');
//
// var run;

module.exports = function(args, done) {
  return done(new Error('Not implemented'));
  // var tasks = {
  //   'update dependencies to latest': run('npm-check-updates -u'),
  //   'install dependencies': run('npm install'),
  //   'check dependencies and style': run('npm run check'),
  //   'run the tests': run('npm test')
  // };
  // var options = {
  //   name: 'install',
  //   spinner: true
  // };
  //
  // taskmgr(tasks, options, done);
};
